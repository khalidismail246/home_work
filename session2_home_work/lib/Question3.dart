// //Declare a var variable and assign a string value to it. Then reassign an integer to the samevariable. Print the variable after each assignment
void main() {
  // data type = dynamic
  var variable;
  variable = "mina";
  print(variable);
  // answer = mina
  variable = 28;
  print(variable);
  //answer = 28
}
