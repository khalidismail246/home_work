// Create a new Dart file. At the top, write a multi-line comment explaining your program. Inside the file, write simple code with proper indentation and single line comments.
void mian() {
  // primitive data type
  // Determines the data type based on the variable.
  // 1 - string
  String named = "khalid ismail";
  // 2 - int = integer
  int age = 25;
  // 3 - double = Fractional numbers
  // no integer
  double height = 185.9;
  // num = number => If I don't know integer numbers or Fractional numbers
  num variable = 15;
  variable = 18.5;
  // bool = boolean => select true or false answer in Question
  bool isLoading = true;
  // dynamic =If I don't know the type of data + Receive all data
  dynamic variable1 = "mohamed";
  variable1 = 15;
  variable1 = 1.5;
  // var = *key word* + select data type + Create variables quickly
  var number1 = "khalid";
  // data type = string;
}
